{
  "scripts": {
    "page:dev": "vuepress dev",
    "page:build": "vuepress build",
    "lambda:dev": "netlify-lambda serve lambda",
    "lambda:build": "netlify-lambda build lambda",
    "build": "npm run page:build && npm run lambda:build"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "netlify-lambda": "^1.4.13",
    "node-sass": "^4.12.0",
    "sass-loader": "^7.1.0",
    "storyblok-node-client": "^1.0.2",
    "vuepress": "^1.0.2"
  }
}
