{
  "scripts": {
    "site:dev": "vuepress dev",
    "site:build": "vuepress build",
    "lambda:dev": "netlify-lambda serve lambda",
    "lambda:build": "netlify-lambda build lambda",
    "build": "npm run site:build && npm run lambda:build"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "netlify-lambda": "^1.4.13",
    "node-sass": "^4.12.0",
    "sass-loader": "^7.1.0",
    "storyblok-js-client": "^1.0.27",
    "vuepress": "^1.0.2"
  }
}
